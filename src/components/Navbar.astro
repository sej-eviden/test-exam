---
import Logo from "./Logo.astro";
interface Props {
  exam?: string;
}
let { exam } = Astro.props;
---

<nav
  class="bg-white border-gray-200 relative dark:bg-gray-800 mx-auto [scrollbar-gutter:stable]"
>
  <div
    class="max-w-screen-lg flex px-6 flex-wrap items-center justify-between mx-auto py-4"
  >
    <a href="/" class="flex items-center">
      <img src="/logo/Logotype_Eviden_RGB_Orange.png" width="250" />
    </a>
    <span class="font-semibold text-4xl text-gray-900 dark:text-lightgray"
      >OneCloud Spain</span
    >
    <div class="block w-auto" id="navbar-dropdown">
      <!-- {
        exam !== undefined ? (
          <h1 class="text-xl font-semibold dark:text-gray-100 leading-3 dark:hover:text-gray-200">
            {exam.toUpperCase().replace("_", " ")}
          </h1>
        ) : ( -->
      <span class="min-w-[18ch] text-xl block"> </span>
      <!-- // <a
          //   href="/contributing"
          //   class="items-center text-xl font-semibold dark:text-gray-100 dark:hover:text-gray-200"
          // >
          //   Contribute
          // </a>
        )
      } -->
    </div>
  </div>
</nav>
