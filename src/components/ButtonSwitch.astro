---
interface Props {
  title: string;
  answer: string;
}
const { title, answer } = Astro.props;
const idTitle = "switch-" + title.split(" ").join("-");
---

<button
  class="appearance-none py-2 px-4 bg-emerald-500 text-white font-semibold rounded-lg shadow-md hover:bg-emerald-700 focus:outline-none focus:ring-1 focus:ring-emerald-500 focus:ring-opacity-75"
  id={idTitle}
>
  Show answer
</button>

<script define:vars={{ idTitle, answer }}>
  const button = document.body.querySelector(`button#${idTitle}`);
  const answerEl = document.body.querySelector(`li#${answer}`);
  const classList = answerEl.classList;

  if (button) {
    button.addEventListener("click", () => {
      if (classList.contains("bg-emerald-200")) {
        classList.remove("bg-emerald-200");
      } else {
        answerEl.classList.add("bg-emerald-200");
      }
    });
  }
</script>
